#include <stdio.h>
#include <cs50.h>

int main(void)
{
    string plaintext = get_string("plaintext: ");
    int ciphertext = ((int) plaintext[0] + key;
    int key = 1;

    printf("%c\n", (char) ciphertext);
}
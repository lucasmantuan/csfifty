#include <stdio.h>
#include <cs50.h>
#include <string.h>

int main(void)
{
    string plaintext = get_string("Plaintext: ");
    string key = "wxy"
    string ciphertext[str]

    if (plaintext[0] == 'a')
    {
        printf("%c\n", plaintext[0]);
    }

    printf("%s\n", plaintext);
}